{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@ShipProof/env": "workspace:*",
    "cofhejs": "^0.3.1",
    "dotenv": "catalog:",
    "hono": "^4.8.2",
    "viem": "^2.47.6",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@ShipProof/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.16.5",
    "typescript": "^5"
  }
}
